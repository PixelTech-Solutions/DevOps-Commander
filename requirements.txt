azure-functions==1.21.3
azure-identity==1.19.0
# Foundry prompt-agent runtime (Responses API) — required for official remote
# MCP tools (Datadog + Grafana). The classic azure-ai-agents SDK has no MCP
# support, so the agent fleet was migrated to azure-ai-projects 2.x.
azure-ai-projects>=2.2.0,<3.0.0
# Action executor: run allow-listed commands on dev VMs via Azure Run Command.
azure-mgmt-compute==33.0.0
# Live observability (SRE): Azure Monitor VM metrics + Log Analytics KQL.
# Pure-python, no native wheels, so it ships cleanly to Linux Consumption.
azure-monitor-query==1.4.1
# Single-use approval tokens for destructive actions (Table Storage nonce store).
azure-data-tables==12.5.0
# Web Chat front end: Bot Framework endpoint (/api/messages). Runs as the
# Function's user-assigned managed identity (UserAssignedMSI) -- no app secret.
# The aiohttp integration package supplies CloudAdapter +
# ConfigurationBotFrameworkAuthentication and pulls a compatible aiohttp
# (3.10.5, manylinux2014/glibc 2.17). Verified to resolve cleanly with the
# pinned cryptography==42.0.8 below (it stays at 42.0.8 -- no glibc bump).
botbuilder-integration-aiohttp==4.16.2
# Pin cryptography to the last release that ships manylinux2014 (glibc 2.17)
# wheels. azure-ai-projects pulls in azure-storage-blob -> cryptography, and a
# newer wheel's Rust binding requires GLIBC_2.33, which the Functions Linux
# Consumption host does not have (ImportError at import time).
cryptography==42.0.8
# Alert notifications: Azure Communication Services email (azure-native, no SMTP
# server / third-party key). Pure-python, ships cleanly to Linux Consumption.
azure-communication-email==1.0.0
